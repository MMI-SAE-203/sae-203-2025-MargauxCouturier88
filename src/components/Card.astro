---
import ImagePB from "./ImagePB.astro";
const { ...film } = Astro.props;
---



<div class="bg-red p-4 rounded-lg shadow-lg text-center group hover:shadow-xl transition-shadow duration-300">
    <ImagePB 
        record={film} 
        field="affiche" 
        alt={film.titre} 
        classPicture="relative w-full h-64 overflow-hidden rounded-lg"
        classImg="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
/>
    <h2 class="text-xl font-bold mt-4 text-gray-800">{film.titre}</h2>
    <p class="text-gray-600">Réalisateur : <span class="font-semibold">{film.realisateur}</span></p>
</div>

